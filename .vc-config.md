# vc-x1 config file

vc-x1 configuration, the bot side. The `toml` fences, concatenated in document order, form
the config ([vc-config-test.md](../vc-config-test.md) is the model rendering). Per-key
documentation: [vc-config.md](../vc-config.md).

The two repos
- work [[1]]
- bot [[2]]
```toml
[repos]
work = ".."
agent = "."
```

# References

[1]: ../vc-config.md#reposwork
[2]: ../vc-config.md#reposbot
